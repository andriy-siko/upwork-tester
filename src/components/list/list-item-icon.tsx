import styled from 'styled-components'

const ListItemIcon = styled.div`
  display: inline-flex;
  align-items: center;
  min-width: 56px;
  flex-shrink: 0;
  position: relative;
`

export default ListItemIcon
